# litelite (work in progress)

A from-scratch SQLite implementation written in Go. It covers lexing, parsing, ASTs, query planning, B+ trees, disk pages, WAL, transactions, and MVCC. Each layer built by hand, one step at a time.

